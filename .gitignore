# macOS
.DS_Store

# Node
node_modules
npm-debug.log*
lerna-debug.log
yarn-error.log

# We use yarn, not npm, so prevent  checking in a
# package-lock.json if someone accidently runs
# npm instead
package-lock.json

### Visual Studio Code ###
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history

# builds
dist/
lib/

# test coverage reports
coverage/
